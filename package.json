{
  "name": "rbkmoney-binbase-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/openapi-cli": "^0.12.7",
    "json-merge-patch": "^0.2.3",
    "json-pointer": "^0.6.0"
  },
  "private": true,
  "scripts": {
    "start": "openapi preview-docs",
    "build": "openapi bundle -o dist",
    "test": "openapi validate"
  }
}
